NODE_ENV=production
PORT=4545
HOST=0.0.0.0

# Đường dẫn thư mục kho nhạc thực tế trên luxaztk-server (440GB FLAC/MP3)
# Hỗ trợ các định dạng: .mp3, .flac, .wav, .m4a, .ogg, .aac
MUSIC_DIR=/home/luxaztk/Music

# Cho phép kết nối CORS từ MeloVista Desktop & Mobile
CORS_ORIGIN=*

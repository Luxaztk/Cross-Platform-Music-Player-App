feat(player): upgrade core audio engine and security landscape
- Implement advanced PlayerProvider with Queue, History (16 songs), Shuffle, and Repeat logic.
- Refactor PlayerBar UI with native range inputs for smooth scrubbing and volume control.
- Add Queue Popover panel with optimized 50-item rendering for performance.
- Implement dynamic Environment-based CSP in Electron main process (Development vs. Production).
- Update PlaylistDetailPage interaction logic (Single click on title to play, remove double-click).
- Create and prioritize project roadmap in root task.md (MVP > Optimization > Visuals > Mobile).
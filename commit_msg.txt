refactor: integrate Inter font offline + project-wide cleanup

Font Integration:
- Add Inter v5.2.8 with @font-face declarations (latin, latin-ext, vietnamese)
- Create fonts.scss with woff2 sources for weights 100-900
- Update typography.scss/json to use 'Inter' as primary font
- Add @music/brand alias in vite.config.ts
- Import brand typography in ThemeProvider.scss

Project Cleanup (post-audit):
- Remove duplicate ILibraryRepository (domain/) and dead LibraryUseCases
- Standardize imports: relative paths → @music/types, @music/core
- Delete 10 legacy .css files (migrated to .scss), fix NotificationProvider import
- Remove dead PlaylistsPage component (no route), unused useLibrary hook
- Remove unused tsconfig.base.json (stale aliases never referenced)
- Delete default Vite assets (react.svg, vite.svg, hero.png)
- Add pickImage to Window.electronAPI type, fix EditModal type safety
- Add README placeholders for packages/hooks, packages/player, packages/ui

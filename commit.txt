feat(core): implement deduplication engine v4 and stabilize test suite

- Implement YouTube URL normalization to strip tracking parameters (si, list, t, etc.)
- Replace fixed 3s duration filter with proportional tolerance (2% of duration, max 15s)
- Add createdAt timestamp to newly imported songs for better audit trails
- Stabilize core test suite by implementing dynamic ID handling and unique mock data
- Achieve 100% pass rate in @music/core across all 34 test cases
- Add explicit test coverage for mixtape tolerance and URL normalization matching

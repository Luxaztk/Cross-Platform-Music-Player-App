feat(downloader): standardize downloader UI and implement bulk metadata editing

- Refactor `DownloaderModal` and `DownloadSection` to use `DownloadPreviewCard` consistently.
- Add visual status indicators (Success, Pending, Downloading) with icons and progress bars.
- Implement `isBulk` mode in `EditModal` to allow batch updating Artist and Album for playlists.
- Disable song editing during the download process to ensure data integrity.
- Fix UI layout issues including vertical alignment and height management in downloader components.
- Resolve multiple TypeScript errors in `DownloadProvider`, `DownloaderUI`, and tests.
- Achieve "Zero Ghost Key" status by adding missing i18n keys and creating `check-i18n-keys.js`.
- Upgrade `ghost-css-variables.js` with professional reporting and unused variable detection.
- Update `____ROADMAP.md` to reflect V5 Downloader and DX improvements.

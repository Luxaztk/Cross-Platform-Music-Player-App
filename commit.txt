feat(desktop): synchronize headphone controls and hardware media keys via MediaSession API

- Implement `useMediaSession` hook in `@music/hooks` to integrate W3C MediaSession API
- Register action handlers for 'play', 'pause', 'previoustrack', 'nexttrack', 'stop', 'seekto', 'seekbackward', and 'seekforward'
- Synchronize `MediaMetadata` (title, artist, album, artwork), `playbackState`, and throttled `positionState` with Windows SMTC and OS transport controls
- Wire `useMediaSession` into `PlayerProvider` for seamless global playback synchronization across the app
- Add `MediaPlayPause`, `MediaTrackNext`, `MediaTrackPrevious`, `MediaStop` support to `useGlobalHotkeys` with input field bypass for media keys
- Set `app.setAppUserModelId` in Electron `main.ts` for proper Windows SMTC app branding
- Add centralized unit tests for `useMediaSession` and media hotkeys (314/314 tests passing)
- Update roadmap tracking in `docs/____ROADMAP.md`

feat(desktop): standardize dropdown UI and centralized testing architecture

- Implement reusable production-grade `CustomDropdown` with Portal rendering.
- Add ARIA accessibility attributes (`listbox`, `option`, `aria-haspopup`, `aria-expanded`).
- Refactor Settings sections (General, Audio, Download, Appearance) to use the new standardized dropdown components.
- Fix event propagation and reactivity bugs in Settings state management.
- Standardize sub-menu headers and menu item dimensions across the application.
- Relocate all unit tests to a centralized `src/tests/` directory with a mirrored architecture.
- Harden unit tests with strict assertions for button types and UI reactivity using `user-event`.
- Update Vitest and RTL configurations for optimized testing environment.

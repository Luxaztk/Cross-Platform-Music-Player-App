feat(core): upgrade audio hash guard to fuzzy sliding window similarity

- Implement ASCII distance-based fuzzy matching to tolerate compression artifacts
  (accuracy scores: dist 0=1.0, 1=0.8, 2=0.4).
- Use sliding window (-10 to +10 offset) in calculateSimilarity to handle temporal jitter.
- Remove over-strict broad-phase substring check for better bitrate resilience.
- Eliminate dangerous split('-')[0] logic to prevent error-fallback collisions.
- Strictly bypass duplication checks for error-fallback hashes to ensure unique storage.

feat(lyrics): implement precision sync system & fix downloader state management

- Implement multi-step lyric offset controls (±1s, ±5s, Reset) with Soft UI toolbar.
- Add 'Sync Now' feature per lyric line for one-click alignment with playback.
- Implement persistent lyric offset via localStorage keyed by YouTube ID (originId).
- Optimize LyricsPanel UI with hover-only visibility for sync controls and slide-in buttons.
- Fix DownloaderModal 'Premature Reset' using useRef to track opening state.
- Implement auto-cleanup for stale download states when editing URL.
- Fix LyricsPanel search query initialization on mount and song change.
- Standardize i18n support for all new synchronization features.

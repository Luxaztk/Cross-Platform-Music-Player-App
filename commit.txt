feat: update project architecture docs and resolve UI styling regressions

- docs: Create and finalize __PROJECT_REPORT.md encompassing the system architecture diagram (Mermaid), monorepo directory structure, MVP definition, and comprehensive module-based task progress.
- fix(PlayerBar): Relocate the QueuePanel component directly inside the .player-right container to perfectly synchronize with the original SCSS structure, permanently resolving the lost styling issue of the Queue without messing with complex nested SCSS rules.
- fix(SongRow): Replace the cross-file Vite SCSS `@extend` directive with static CSS properties (background, backdrop-filter, border) and inject `flex-direction`, ensuring the Song Row Context Menu (more-menu) renders correctly without missing backgrounds or breaking structure when rendered via React Portal.

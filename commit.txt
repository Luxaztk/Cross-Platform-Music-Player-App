feat(desktop): implement professional bulk actions and refined SongRow UX

- Rewrite SongRow interaction model: unified title/icon click for standard playback, row click for selection.
- Implement high-performance Bulk Actions Bar using React Portals to eliminate initial layout shifts.
- Standardize centering logic using a Flexbox wrapper (fixed positioning with left/right: 0) to avoid scrollbar traps.
- Add "Add to Queue" bulk operation with strict visual (top-to-bottom) ordering logic.
- Optimize entry animations by unifying transforms to purely Y-axis transitions (slideUpOnly).
- Refine visual hierarchy with brand primary colors for positive actions and danger red for destructive/cancel hovers.
- Fix Ephemeral Hint logic for Lyrics Panel to improve discoverability of hidden controls.

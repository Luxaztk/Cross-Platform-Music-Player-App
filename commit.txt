feat(logging): implement isomorphic tracing system with production stripping

- Integrated 'electron-log' with a deterministic path: %AppData%/MeloVista/logs/main.log.
- Built an isomorphic 'Logger' utility in packages/utils to safely bridge Main and Renderer logs.
- Configured Vite 8 (Rolldown/Oxc) to physically strip console/debugger in production builds.
- Injected granular diagnostic tracing into YoutubeDownloader and LibraryService to debug Guard 3 race conditions.
- Fixed monorepo hoisting errors by installing electron at root and cleared release directory locks.

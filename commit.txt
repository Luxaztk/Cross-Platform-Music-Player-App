fix(server,client): resolve zero-song sync state, prune orphan stream songs, and protect playback loop

- Server & Storage Engine:
  - Fix GET /api/health to calculate totalSongs via scanner.getTotalSongsCount(), avoiding false 0 counts when tracks have private/whitelist visibility.
  - Add userAccessibleSongs metric to health response and pass client username in ServerClient.checkHealth.
  - Integrate storage.pruneOrphans() into MusicScanner to automatically clean up database records when physical audio files are deleted on disk.
  - Standardize music directory paths in ecosystem.config.cjs with safe fallback between apps/server/data/music and data/music.

- Desktop & Mobile Client Sync:
  - Automatically prune all stream tracks belonging to server from library when server returns 0 songs during sync (preserving local tracks).
  - Automatically detect and prune orphan stream tracks (orphanStreamSongIds) when server songs are removed and updated.
  - Refresh server health status after sync to instantly reflect updated song counts (0 tracks) in settings badge.
  - Add friendly Empty State illustration in ServerLibraryBrowserModal when server contains no songs.
  - Resolve React Compiler preserve-manual-memoization error in ServerSection by extracting serverToken as a primitive binding.

- Audio Engine & Player:
  - Differentiate stream unavailable error (player.streamUnavailable) from missing local file error (player.fileNotFound) in PlayerWithLibrary.tsx.
  - Implement consecutive failure circuit breaker (consecutiveFailuresRef, MAX_CONSECUTIVE_FAILURES = 5) in PlayerProvider.tsx to prevent infinite auto-skip loops and CPU freezing.

- Quality, Types & Roadmap:
  - Add zero-song state and orphan cleanup test suite to server.test.ts (23/23 tests Green).
  - Add zero-song library cleanup and orphan pruning tests in ServerSection.test.tsx (16/16 tests Green).
  - Fix duplicate player key in packages/i18n/src/desktop.ts and enforce zero TypeScript errors (npx tsc -b passes clean).
  - Mark Zero-Song Sync & Stream Orphan Cleanup task as completed in docs/____ROADMAP.md.

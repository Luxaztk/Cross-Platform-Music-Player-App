Refactor deploy script and enable auto-publishing.

- Fix UTF-8 BOM and character encoding issues when reading commit.txt on Windows.
- Use temporary file for git commit to prevent shell escaping errors.
- Shift electron-builder execution context to apps/desktop directory.
- Set 'draft: false' to automatically publish releases to GitHub.

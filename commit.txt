feat(desktop): implement standalone yt-dlp binary resolution

- Bypass system Python requirement via YOUTUBE_DL_SKIP_PYTHON_CHECK=1.
- Dynamically resolve yt-dlp path for both dev and prod (asarUnpacked).
- Add robust unit tests for path resolution logic using Vitest doMock.
- Fix existing test failures in MetadataManager and YoutubeDownloader.
- Add CI workflow for automated desktop unit testing.

This change ensures that end-users do not need to install Python to use the
online download feature, improving the standalone portability of the app.

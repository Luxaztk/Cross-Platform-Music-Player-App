Refactor deploy pipeline and enable auto-publishing.

- Fix: Use 'releaseType: release' in electron-builder config to resolve schema errors and enable automatic publishing.
- Fix: Handle UTF-8 BOM and encoding issues when reading commit.txt on Windows.
- Chore: Shift execution context to apps/desktop and use temporary files for robust git commits.

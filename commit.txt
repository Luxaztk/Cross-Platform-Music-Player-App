feat: UI/UX hardening and stability improvements

- Fix UI Flash in LyricsPanel by removing redundant fadeIn animations and delaying hints
- Eliminate startup flicker in SettingsProvider by removing ephemeral loading text
- Optimize DownloadPreviewCard layout: album tags now expand to full width when status badges are absent
- Improve DownloadModal: titles are now properly truncated with ellipsis if too long
- Re-order DownloadSection: moved auto-import folder settings to the bottom for better hierarchy
- Standardize i18n: replaced remaining plain text with `t()` calls for 100% translation coverage
- Build & Test Stabilization: fixed critical test failures in Sidebar, PlayerBar, and DownloaderModal, and resolved unused variable build errors

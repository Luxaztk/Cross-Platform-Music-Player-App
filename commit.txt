fix(bot): complete deep audit hardening, bug fixes (P0-P3) and deployment readiness

- Fix voice connection reconnect race condition and limit playNext recursion
- Fix FFmpeg DSP filter arguments order and YouTube flat-playlist URL extraction
- Mitigate unhandled WebSocket exceptions and enforce MAX_QUEUE_SIZE = 500
- Eliminate ActivityServer 1s ticker broadcast storm with event-driven state sync
- Add 500ms debounce to Now Playing message updates to prevent Discord 429 rate limits
- Implement Netscape cookie expiration check in YoutubeExtractor
- Implement auto-rejoin on Gateway ShardResume and VoiceStateUpdate
- Add button prefix guard ('btn_') and Slash Commands SHA-256 caching
- Enhance /api/health with system resource metrics and configure PM2 ecosystem
- Update unit tests (19/19 passing) and project roadmap documentation
